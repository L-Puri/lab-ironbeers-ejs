    <!-- const beersFromApi = async () => {
        const beer = []
        try {
        fromAPI.forEach((name, tagname) => {
            batchPromises.push(name('beer', nameIndex))
        })
    
        const results = await Promise.all(batchPromises)
        results.forEach(result => {
            document.querySelector('beersFromApi')
        } catch (error) {
        console.log(error)
        }
    }
    
    beersFromApi(); -->